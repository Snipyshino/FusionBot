// import {Pipe, PipeTransform} from '@angular/core';
// import {Anomaly} from './core/models/Anomaly';
//
// @Pipe({
//     name: 'anomalyCheckedPipe'
// })
// export class AnomalyCheckedPipePipe implements PipeTransform {
//
//     transform(anomalyList: Array<Anomaly>, showChecked: boolean = false): any {
//         return anomalyList.filter(
//             (anomaly: Anomaly, index: number) => anomaly.checked === showChecked
//         );
//     }
// }
